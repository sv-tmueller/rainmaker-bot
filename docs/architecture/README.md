# Architecture

Stack and policy decisions, the data model, and the domain math (the
distribution, calibration, outcome-probability, and edge-ranking logic). Locked
unless explicitly revisited.

At pre-implementation these live in the MVP 1.0 design spec
(`../superpowers/specs/2026-05-29-mvp1-advisory-design.md`). Extract them here as
the code lands, and when code and a note disagree, the code wins and the note is
corrected in the same PR.
